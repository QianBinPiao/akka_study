package com.egn.finitedurationexample

import concurrent.duration._


object FiniteDurationExample extends App {
  val maxTime = Duration(10, SECONDS)
  //val amount = concurrent.Await.result(5, maxTime)

}
